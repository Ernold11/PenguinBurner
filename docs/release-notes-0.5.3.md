# PenguinBurner 0.5.3

## Changes

- Adds the self-hosted Flatpak version and one-command install path.
- Adds the new sudo daemon for the undervolting process, giving the desktop UI
  and Flatpak build a persistent privileged hardware service instead of repeated
  per-action prompts.
